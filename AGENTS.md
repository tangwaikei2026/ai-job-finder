# Codex 全局执行规则：每个批次都必须遵守

## 项目与当前范围

- 当前项目：`tangwaikei2026/ai-job-finder`
- 目标：围绕 `src` 建立稳定采集主路径，并逐步平滑去掉旧路径代码。
- 当前阶段只处理 raw 采集层，不处理业务过滤、diff、README、数据库写入、通知。

## 总原则

1. 每次只执行用户给出的当前批次任务。
2. 不允许一次性完成后续批次。
3. 当前批次完成后必须停止，等待用户确认。
4. 不主动扩大修改范围。
5. 不真实访问招聘网站。
6. 不修改业务过滤逻辑。
7. 不修改 README。
8. 不修改数据库写入逻辑。
9. 不修改 diff 逻辑。
10. 不修改通知逻辑。
11. 不提交 commit，除非用户明确要求。
12. 如果发现当前代码结构和任务假设不一致，先说明差异，再给出最小修改方案，不强行大改。

## 仓库实现约束

- 开工先检查当前分支、工作区状态及相对主分支的 diff。
- 平台逻辑只放独立 adapter，不改 `main.py` 或其他 adapter。
- 简单参数放入配置；新平台默认关闭，除非任务明确启用。
- raw 层不复制岗位池规则。
- 平台失败须返回 manifest，不得击穿 pipeline。
- 全失败不得用空结果覆盖旧产物。
- 浏览器方案须先证明 HTTP 不可行，并确保资源释放。
- 测试覆盖正常、去重、结构变化，并跑全量回归。
- 生成数据、日志和临时文件不得进入功能提交。

## 每个批次完成后的必需输出

每个批次结束时必须输出：

1. 本批次修改了哪些文件。
2. 每个文件为什么要改。
3. 运行过哪些命令。
4. 命令结果是否通过。
5. `git status --short`。
6. `git diff --stat`。
7. 关键文件的 `git diff`。
8. 是否存在风险或未解决问题。
9. 明确写出：`本批次已完成，已停止，等待人工确认后再继续下一批次。`

## 每个批次的固定验收命令

能运行则运行：

```bash
python -m compileall src tests
pytest -q
```

如果当前仓库测试太多或已有旧测试失败，则至少运行：

```bash
python -m compileall src tests
pytest tests -q
```

如果 `tests` 还不存在，必须说明原因。

## Diff 输出要求

必须输出：

```bash
git status --short
git diff --stat
```

并针对本批次修改的关键文件输出：

```bash
git diff -- <file_path>
```

如果 diff 很长，可以只输出关键片段，但必须说明可用什么命令查看完整 diff。

## 停止规则

- 完成当前批次后，不继续下一批次。
- 不说“我接下来会继续做”。
- 必须停在当前批次验收结果。
